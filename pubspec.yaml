# 🔄 Updated for SomTam (Thai version, safe build config - with i18n + ui + words image fix)

name: somtam
description: Somtam – Thai flashcards & phrases
version: 1.0.0+2
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  scrollable_positioned_list: ^0.3.8
  video_player: ^2.8.6
  shared_preferences: ^2.3.2
  file_picker: ^10.3.2
  path_provider: ^2.1.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/cards.json
    - assets/images/ui/      # 👉 App logos, splash images
    - assets/images/words/      # 👉 Word-level images (0001.zero.jpg etc.)
    - assets/audio/context/
    - assets/audio/thai/   
    - assets/fonts/
    - assets/i18n/               # 👉 Language JSONs (en.json, th.json etc.)

  fonts:
    - family: EBGaramond
      fonts:
        - asset: assets/fonts/EBGaramond/EBGaramond-Regular.ttf
        - asset: assets/fonts/EBGaramond/EBGaramond-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/EBGaramond/EBGaramond-Bold.ttf
          weight: 700
        - asset: assets/fonts/EBGaramond/EBGaramond-Italic.ttf
          style: italic
